{% extends 'base.html.twig' %}

{% block title %}New Livre
{% endblock %}

{% block body %}
	{% if is_granted('ROLE_ADMIN') %}

		<h1>Create new Livre</h1>

		{{ include('livre/_form.html.twig') }}
		<br>
		<a href="{{ path('livre_index') }}" class="btn btn-secondary">back to list</a>
	{% else %}
		<h1>404 page</h1>
	{% endif %}
{% endblock %}
