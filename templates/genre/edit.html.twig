{% extends 'base.html.twig' %}

{% block title %}Edit Genre
{% endblock %}
{% block body %}
	<style>
		body {
			margin: 40px;
		}
	</style>
	<br>
	<table>
		{% if is_granted('ROLE_ADMIN') %}
			<h2>Edit Genre</h2>
			<center>
				<a class="btn  m-12" style="width:50%; heigth:50%">
					<i class="bi bi-pen">
						{{ include('genre/_form.html.twig', {'button_label': 'Modifier'}) }}
					</i>
					<button class="btn">
						{{ include('genre/_delete_form.html.twig') }}
					</button>
				</a>
			</center>
			<a href="{{ path('genre_index') }}" class="btn btn-secondary" style="margin-left:30px">back to list</a>
		</table>
	{% endif %}
{% endblock %}
