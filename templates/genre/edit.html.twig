{% extends 'base.html.twig' %}

{% block title %}Edit Genre
{% endblock %}


{% block body %}
	{% if is_granted('ROLE_ADMIN') %}
		<h1>Edit Genre</h1>

		{{ include('genre/_form.html.twig', {'button_label': 'Update'}) }}

		<a href="{{ path('genre_index') }}" class="btn btn-secondary">back to list</a>

		<a class="btn btn-danger">{{ include('genre/_delete_form.html.twig') }}</a>

	{% else %}
		<h1>404 page</h1>
	{% endif %}

{% endblock %}
