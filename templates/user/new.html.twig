{% extends 'base.html.twig' %}

{% block body %}
	<table>
		{% if is_granted('ROLE_ADMIN') %}
			<br>
			<h2>Nouveau Utilisateur</h2>
			<center>
				<a class="btn  m-12" style="width:50%; heigth:50%">
					<i class="bi bi-plus">
						{{ include('user/_form.html.twig' , {'button_label': 'Enregister'}) }}
					</i>
				</a>
			</center>
			<a href="{{ path('user_index') }}" class="btn btn-secondary" style="margin-left:30px">back to list</a>
		</table>
	{% else %}
		<h1>404 page</h1>
	{% endif %}
{% endblock %}
